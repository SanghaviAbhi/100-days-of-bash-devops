# Exercise 1 — Print Basic Environment Info

Goal: Create a small script that prints the current user, home directory, and working directory.

Acceptance Criteria
- Script named `exercise1.sh` placed in `day-01/exercises/`.
- When run, it prints lines containing `User:`, `Home:`, and `CWD:` with correct values.
- Should not require `sudo`.

Hints
- Use `$USER`, `$HOME`, and `pwd`.
